services:
  web:
    build: .
    ports:
      - "8002:8002"
    volumes:
      - .:/app
      - ./db:/app/db
    env_file:
      - .env
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8002"
